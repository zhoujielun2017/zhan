用户收货地址列表
---
tags:
  - user_address
parameters:
  - name: page
    in: query
    default: 1
  - name: page_size
    in: query
    default: 10
responses:
  200:
    description: A list of colors (may be filtered by palette)
    schema:
      type: object
      properties:
        code:
          type: string
          enum: ['success', 'rgb', 'cmyk']

