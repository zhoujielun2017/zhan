更新用户头像
---
tags:
  - user
parameters:
  - name: body
    in: body
    description: JSON parameters.
    schema:
      properties:
        head_url:
          type: string
          description: head_url.
          example: /head_url/head_url.jpg
responses:
  200:
    description: description
    schema:
      type: object
      properties:
        code:
          type: string
          enum: ['success','error.head.null','error.not.exists']
          description: ''
