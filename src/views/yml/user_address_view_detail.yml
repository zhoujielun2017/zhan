根据id获取用户收货地址(用户使用)
---
tags:
  - user_address
parameters:
  - name: id
    in: path
    description: JSON parameters.
    schema:
      properties:
        id:
          type: string
          example: ppppp
responses:
  200:
    description: description
    schema:
      type: object
      properties:
        code:
          type: string
          enum: ['success','error.not.yours']
          description: ''
